import { PrismaClient } from '../generated/prisma/client'
 const prismaClient = new PrismaClient()

 export default prismaClient;
// use `prisma` in your application to read and write data in your DBs